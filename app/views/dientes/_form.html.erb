<%= form_for(@diente) do |f| %>
  <% if @diente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diente.errors.count, "error") %> prohibited this diente from being saved:</h2>

      <ul>
      <% @diente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %><br>
    <%= f.text_field :numero %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
